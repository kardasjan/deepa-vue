export default {
  getNotifications (store) {
    return store.notifications
  },
  getSiteList (store) {
    return store.sites
  },
  selectedSite (store) {
    return store.selectedSite
  },
  sites (store) {
    return store.sites
  }
}
